name: stg_licensing
description: Shared STG portfolio trial, subscription licensing, and pricing.
publish_to: 'none'

version: 1.0.0

environment:
  sdk: ^3.4.0

dependencies:
  flutter:
    sdk: flutter
  crypto: ^3.0.6
  flutter_riverpod: ^3.3.1
  intl: ^0.20.2
  shared_preferences: ^2.5.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
